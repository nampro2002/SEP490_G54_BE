CREATE TABLE `smarthealthc`.`code` (
                    `id` INT NOT NULL AUTO_INCREMENT,
                    `email` VARCHAR(225) NOT NULL,
                    `code` VARCHAR(225) NOT NULL,
                    PRIMARY KEY (`id`));
